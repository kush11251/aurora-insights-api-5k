# Aurora Insights API
A fast and scalable data analytics platform.

## Installation
To install the project, run the following command:

npm install

## Running the Project
To run the project, execute the following command:

npm start